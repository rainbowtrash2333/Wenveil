# Test fixtures

Files in this directory are small, synthetic regression fixtures. They must not contain copied OCR output,
client documents, private filenames, encrypted mappings, passwords, or production data. Use temporary files
under `test-artifacts/` for authorized local acceptance tests.
